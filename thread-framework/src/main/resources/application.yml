info:
  build:
    artifact: multithread-framework
    name: multithread-framework
    description: multithread-framework
    version: '@project.version@'

management:
  port: 31121
  security:
    enabled: false

server:
  port: 11121
  
security:
  user:
    password: null
  basic:
    enabled: false
  

spring:
  
  thymeleaf:
    cache: false
    mode: LEGACYHTML5

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    com.englishasl: DEBUG 
